import React from 'react';
import express from 'express';
import {user, appointment, id} from '';


const connect = {express} 
const specificApp = 

const showAppointments () => {
  connect.get('/appointments')
}


const postAppointments () => {
  connect.post($)
}
